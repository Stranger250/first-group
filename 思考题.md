1.防止多线程同时操作和读取同一个数据导致数据读写错误，得到错误的结果

2.线程在执行代码时必须要有全局解释器锁GIL，该代码模拟的是频繁的I/O请求，调用sleep时系统会释放GIL让出cpu给其他线程，纯CPU场景下则因为只有一个线程拥有GIL，其他线程都在等待GIL，导致线程频繁争夺GIL，反而消耗了额外的时间，反而消耗更长的时间

3.是接口的设计思想，作用是将方法的实现交给子类，子类能根据自己的实际情况设计方法，假如子类没有实现，也不会出现后续调试时找不到出现问题的位置导致花掉额外的时间开销


